{
  "name": "@mycelium-ink/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.json && electron .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mycelium-ink/core": "workspace:*"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "typescript": "^5.6.3"
  }
}
